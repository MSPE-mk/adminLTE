export class Reclamation {
  constructor(
    private year?: Number,
    private month?: String,
    private nb_resolved?: Number,
    private objective?: Number,
    private nb_not_resolved?: Number
  ) {}
}
